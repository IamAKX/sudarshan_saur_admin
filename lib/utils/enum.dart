// ignore_for_file: constant_identifier_names

enum UserStatus { CREATED, ACTIVE, SUSPENDED, BLOCKED }

enum UserType { STOCKIST, DEALER, CUSTOMER, OTHERS }

enum AllocationStatus {
  PENDING,
  INITIATED,
  PROCESSING,
  ALLOCATED,
  APPROVED,
  DECLINED
}
